/*
  let VS var VS const
  let -> temp, changeable.
  const - not changeable.
        - must be initialized
  var -> global, changeable.
*/

/*
  String, Numbers, Boolean, null, undefined
  - Number can be int or float.
  - null is empty
  let a; -> undefined

  to test a type: 
  console.log(typeof <variable>)
*/

/*
  to output strings... old way: console.log("first " + name + "last: " + last)
  to output strings... old way: console.log("first ${name} last ${last})

*/

/*
  Arrays

  const numbers = new Array(1,2,3,4,5)
  const moreNumbers = [1,2,3,4,5]
  
*/

/*

*/

/*
*/